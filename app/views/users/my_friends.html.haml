%h1 Friends

%div.search-area
  %h3 Search Friends
  = form_tag search_friend_path, remote: true, method: :get do
    %div.form-group.row
      %div.col-sm-9.no-right-padding
        = text_field_tag :friend, params[:friend],
        placeholder: 'Name or email', autofocus: true,
        class: 'form-control form-control-lg'
      %div.col-sm-3.no-left-padding
        = button_tag type: :submit, class: 'btn btn-success' do
          = fa_icon 'search 2x'

%div{ id: "friend-results" }

- if !@friends.empty?
  = render 'friends/list'
